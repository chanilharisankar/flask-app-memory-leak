trigger:
  - master

pool:
  vmImage: 'ubuntu-latest'

variables:
  ios_run: 
  
stages:
  - stage: android
    pool:
      vmImage: 'ubuntu-16.04'
    jobs:
      - job: A1
        steps:
        - bash: echo "A"

  - stage: ios
    condition: eq(variables.ios_run, true)
    pool:
      vmImage: 'ubuntu-latest'
    jobs:
      - job: B1
        steps:
        - bash: echo "B"
    